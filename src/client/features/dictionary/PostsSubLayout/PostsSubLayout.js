import React from 'react';
import { withStyles } from '@material-ui/core';

import styles from '../styles';

const PostsSubLayout = ({ classes }) => (
  <main className={classes.layout}>PostsSubLayout</main>
);

export default withStyles(styles)(PostsSubLayout);
