import React from 'react';
import { withStyles } from '@material-ui/core';

import styles from '../styles';

const SeniorityTypesSubLayout = ({ classes }) => (
  <main className={classes.layout}>SeniorityTypesSubLayout</main>
);

export default withStyles(styles)(SeniorityTypesSubLayout);
