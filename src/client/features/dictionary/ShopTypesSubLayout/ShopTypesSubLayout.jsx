import React from 'react';
import { withStyles } from '@material-ui/core';

import styles from '../styles';

const ShopTypesSubLayout = ({ classes }) => (
  <main className={classes.layout}>ShopTypesSubLayout</main>
);

export default withStyles(styles)(ShopTypesSubLayout);
