import React from 'react';
import { withStyles } from '@material-ui/core';

import styles from '../styles';

const ShopsSubLayout = ({ classes }) => (
  <main className={classes.layout}>ShopsSubLayout</main>
);

export default withStyles(styles)(ShopsSubLayout);
