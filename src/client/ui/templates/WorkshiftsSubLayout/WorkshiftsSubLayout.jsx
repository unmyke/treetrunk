import React from 'react';
import { withStyles } from '@material-ui/core';

import styles from '../styles';

const WorkshiftsSubLayout = ({ classes }) => (
  <main className={classes.layout}>WorkshiftsSubLayout</main>
);

export default withStyles(styles)(WorkshiftsSubLayout);
